services:
  api:
    build: .

    ports:
      - "3001:3001"

    env_file:
      - .env

    depends_on:
      - mongodb

  mongodb:
    image: mongo:8

    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin123
